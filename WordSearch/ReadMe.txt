This is a simple wordsearch program written in Java. 

To compile the program into an executable jar, run the compileJar.sh script from the terminal or command line. 

To execute the program, run the runWordSearch.sh script from the teminal or command line, passing the full file path of the file to be assessed as an argument.

IE
sh runWordSearch.sh testData/secondTestFile.txt